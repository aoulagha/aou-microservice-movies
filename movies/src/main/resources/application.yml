server:
  port: 8080

spring:
  application:
    name: movie
  datasource:
    url: jdbc:postgresql://localhost:5432/movies
    username: movie_user
    password: nevermind
  jpa:
    database: POSTGRESQL
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true