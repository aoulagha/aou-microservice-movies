server:
  port: 8081

spring:
  application:
    name: artist
  datasource:
    url: jdbc:postgresql://localhost:5432/artists
    username: movie_user
    password: nevermind
  jpa:
    database: POSTGRESQL
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true